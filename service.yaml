apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80 # External service port
      targetPort: 8000 # Container port
  type: LoadBalancer
